<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.memoirs.travel.travelmemoirs.MemoirDetails">

    <VideoView
        android:id="@+id/memoir_detail_video"
        android:layout_width="match_parent"
        android:layout_height="200dp" />

    <ImageView
        android:id="@+id/memoir_detail_thumbnail"
        android:layout_width="match_parent"
        android:layout_height="200dp" />

    <RatingBar
        android:id="@+id/memoir_detail_rating"
        android:layout_width="150dp"
        android:layout_height="20dp"
        android:layout_below="@+id/memoir_detail_video"
        android:layout_alignParentLeft="true"
        />

    <TextView
        android:id="@+id/memoir_detail_date"
        android:layout_width="150dp"
        android:layout_height="20dp"
        android:layout_below="@id/memoir_detail_video"
        android:layout_alignParentRight="true"
        />

    <TextView
        android:id="@+id/memoir_detail_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/memoir_detail_rating"
        android:hint="@string/no_description"
        />



</RelativeLayout>
